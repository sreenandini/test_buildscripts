using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace BMC.EnterpriseClient.Views
{
    public enum FormEditTypes
    {
        Add = 0,
        Edit = 1,
        List = 2,
        Delete=3
    }

    public enum FormGroupTypes
    {
        Group = 0,
        Item = 1
    }

    public enum AboutFormTypes
    {
        About = 0,
        Splash = 1
    }
}
